GITHUB_TOKEN=
ANTHROPIC_BASE_URL=
ANTHROPIC_AUTH_TOKEN=
ANTHROPIC_MODEL=claude-opus-4.7
SITE_URL=https://example.github.io
LLM_CALL_BUDGET=600
# GoatCounter site code (subdomain prefix, e.g. "aiweekly" for aiweekly.goatcounter.com).
# Leave blank locally to skip injection. Set as GitHub Actions Variable (NOT Secret) for prod
# — value is inlined into the client bundle and is public by design.
PUBLIC_GOATCOUNTER_CODE=
